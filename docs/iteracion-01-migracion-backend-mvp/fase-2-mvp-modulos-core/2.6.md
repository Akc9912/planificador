# 2.6 Calculo de Progreso

## Objetivo

Implementar el calculo de progreso academico por carrera, incluyendo materias aprobadas, pendientes, cursando, creditos acumulados, porcentaje de avance y estadisticas derivadas.

## Por que se hace

El progreso es uno de los principales valores del backend frente al frontend y frente al uso directo de Supabase. Sin esta logica, el MVP queda como CRUD con poco valor agregado.

## Pasos de ejecucion

1. Definir el DTO de progreso y el servicio responsable.
2. Calcular metricas por cantidad de materias y por creditos, si aplica.
3. Reusar el estado de correlativas y materias aprobadas como base.
4. Exponer endpoint de progreso y estadisticas.
5. Agregar pruebas de calculo.

## Comandos exactos

```bash
grep -R "CareerProgressResponseDto\|getCareerProgress\|/career/{careerId}/progress" -n src/main/java/aktech/planificador/modules/subject
./mvnw -q -DskipTests compile
./mvnw -q -DskipTests=false -Dtest=SubjectServiceTest,SubjectControllerTest,ModuleBoundariesTest test
```

## Ejemplo de codigo

```java
double rawPercentage = (numerator * 100.0) / denominator;
return Math.round(rawPercentage * 100.0) / 100.0;
```

## Contrato implementado en Subject

- `GET /subjects/career/{careerId}/progress`
        - Devuelve progreso academico consolidado por carrera.
        - Incluye: conteos por estado (`approved`, `pending`, `inProgress`, `regular`, `libre`), materias bloqueadas/habilitadas y metricas de creditos/horas acumulados.
        - Fuente de verdad: `SubjectService#getCareerProgress`.

## Verificacion

- El backend puede devolver porcentaje y contadores de avance.
- Los calculos no dependen de clases legacy.
- Existen pruebas con carreras vacias y con datos parciales.

## Evidencia de ejecucion - 17/03/2026

- Comandos ejecutados:

```bash
./mvnw -q -DskipTests compile
./mvnw -q -DskipTests=false -Dtest=SubjectServiceTest,SubjectControllerTest,ModuleBoundariesTest test
```

- Reportes Surefire en verde:
- `TEST-aktech.planificador.modules.subject.service.SubjectServiceTest.xml`: `tests=12`, `errors=0`, `failures=0`.
- `TEST-aktech.planificador.modules.subject.controller.SubjectControllerTest.xml`: `tests=12`, `errors=0`, `failures=0`.
- `TEST-aktech.planificador.architecture.ModuleBoundariesTest.xml`: `tests=2`, `errors=0`, `failures=0`.
- Total corrida focalizada: `26 tests`, `0 failures`, `0 errors`.

## Resultado esperado

La API puede responder progreso real por carrera y alimentar vistas de resumen sin calculos duplicados en frontend.

## Criterio de cierre

- [x] Existe DTO de progreso.
- [x] Existe servicio de calculo.
- [x] Existe endpoint asociado.
- [x] Hay pruebas de escenarios basicos y borde.

## Estado de la tarea

Completada (17/03/2026).

## Troubleshooting breve

Si aparece una metrica nueva, agregarla en el servicio de progreso. No repartir calculos parciales en varios controladores porque eso rompe consistencia.
