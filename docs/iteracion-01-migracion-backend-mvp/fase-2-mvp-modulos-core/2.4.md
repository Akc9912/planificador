# 2.4 Implementar Auth Module

## Objetivo

Consolidar el modulo `auth` como unico punto de autenticacion modular del backend, manteniendo validacion JWT, reglas de seguridad y contratos listos para handoff con frontend cuando corresponda.

## Por que se hace

Auth ya tiene avance real. Esta tarea no es rehacerlo, sino estabilizarlo y dejarlo documentado como modulo cerrado para no reabrir discusiones cuando entren los modulos restantes.

## Pasos de ejecucion

1. Auditar clases activas del modulo auth.
2. Confirmar que login, register y cambio de password siguen delegados a Supabase cuando aplique.
3. Confirmar que no hay clases legacy de auth reintroducidas.
4. Ejecutar la suite de auth y seguridad.
5. Dejar documentado el contrato que consumira frontend cuando toque su migracion.

## Comandos exactos

```bash
find src/main/java/aktech/planificador/modules/auth -type f | sort
find src/test/java/aktech/planificador/modules/auth -type f | sort
./mvnw -q -DskipTests=false -Dtest=AuthSessionServiceTest,AuthJwtAuthenticationFilterTest,AuthModuleControllerTest,SecurityConfigAuthIntegrationTest test
grep -R "Supabase\|410 GONE\|change-password\|login\|register" -n src/main/java/aktech/planificador/modules/auth src/main/java/aktech/planificador/Config
```

## Ejemplo de codigo

```java
@PostMapping("/token/validate")
public LoginResponseDto validateToken(@RequestBody TokenValidationRequestDto request) {
    return authSessionService.getSessionFromToken(request.getAccessToken());
}
```

## Contrato auth para handoff frontend

| Endpoint | Auth requerida | Respuesta esperada | Fuente de verdad |
| --- | --- | --- | --- |
| `GET /auth/me` | Si (Bearer JWT valido) | `200` + `LoginResponseDto` | `AuthModuleController#me` + `AuthSessionService#getSessionFromAuthorizationHeader` |
| `POST /auth/token/validate` | No (token va en body) | `200` + `LoginResponseDto` | `AuthModuleController#validateToken` |
| `POST /auth/login` | No | `410 GONE` + `{ message, operation }` | `AuthModuleController#login` |
| `POST /auth/register` | No | `410 GONE` + `{ message, operation }` | `AuthModuleController#register` |
| `POST /auth/register-admin` | Si (`ROLE_ADMIN`) | `410 GONE` para admin, `403` para no-admin/anonimo | `SecurityConfig` + `AuthModuleController#registerAdmin` |
| `PUT /auth/change-password` | No | `410 GONE` + `{ message, operation }` | `AuthModuleController#changePassword` |

## Evidencia de cierre - 17/03/2026

- Suite de auth y seguridad ejecutada en verde:

```bash
./mvnw -q -DskipTests=false -Dtest=AuthSessionServiceTest,AuthJwtAuthenticationFilterTest,AuthModuleControllerTest,SecurityConfigAuthIntegrationTest,ModuleBoundariesTest test
```

- Resultado consolidado de la corrida focalizada:
    - AuthSessionServiceTest
    - AuthJwtAuthenticationFilterTest
    - AuthModuleControllerTest
    - SecurityConfigAuthIntegrationTest
    - ModuleBoundariesTest
    - Total: `29 tests`, `0 failures`, `0 errors`.

- Validaciones de arquitectura y legacy:
    - Existe un unico controlador auth modular en `modules/auth`.
    - `SecurityConfig` mantiene `"/legacy/auth/**"` en `denyAll()`.
    - No se detectan imports del modulo auth hacia capas legacy prohibidas.

## Verificacion

- Existe una sola implementacion auth vigente para codigo modular.
- Las pruebas del modulo auth quedan en verde.
- Las reglas de seguridad siguen aplicadas.
- El contrato auth queda listo para ser consumido por frontend cuando toque su migracion.

## Resultado esperado

Auth queda cerrado, estable y sin mezcla con codigo legacy, listo para acompañar el resto del MVP sin convertirse en fuente de regresiones.

## Criterio de cierre

- [x] Solo queda activa la implementacion modular de auth.
- [x] La suite de auth y seguridad pasa en verde.
- [x] El contrato de endpoints auth esta claro.
- [x] No se reintroduce codigo legacy en auth.

## Estado de la tarea

Completada (17/03/2026).

## Troubleshooting breve

Si aparece una necesidad nueva de auth, agregarla al modulo `modules/auth`. No revivir controladores o servicios legacy como solucion rapida.
