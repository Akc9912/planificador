# 3.2 - Implementar reminder_subjects con asociacion opcional

## Objetivo

Implementar relacion opcional entre reminders y subjects usando tabla reminder_subjects.

## Razon Tecnica

Permite recordatorios generales o vinculados a materias especificas sin forzar acoplamiento.

## Comandos Exactos

```bash
rg -n "CREATE TABLE public\.reminder_subjects|CREATE POLICY .*reminder_subjects" humanis_db_init.sql
./mvnw -DskipTests=false -Dtest=*ReminderSubject*Test test
```

## Ejemplo De Codigo (Si Aplica)

```txt
Operaciones sugeridas:
POST /reminders/{reminderId}/subjects/{subjectId}
DELETE /reminders/{reminderId}/subjects/{subjectId}
GET /reminders/{reminderId}/subjects
```

## Verificacion

1. Asociacion reminder-subject respeta ownership comun.
2. Reminder sin subjects sigue siendo valido.
3. No se permiten duplicados en la relacion.

## Resultado Esperado

Relacion opcional reminder-subject implementada y testeada.

## Criterio De Cierre

- [ ] objetivo cumplido
- [ ] verificacion ejecutada
- [ ] evidencia registrada
- [ ] documentacion relacionada actualizada

## Troubleshooting Breve (Si Aplica)

- si falla integridad referencial: revisar FKs y orden de borrado.
- si falla ownership cruzado: validar parent reminder antes de asociar.
